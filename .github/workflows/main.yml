name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 20.19.0 is the declared floor: it's the first Node that enables
        # require(esm) by default, which this ESM-only package relies on.
        node-version:
          - 20.19.0
          - lts/*
    steps:
      - uses: actions/checkout@v3.3.0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm format
      - run: pnpm build
      - run: pnpm test
